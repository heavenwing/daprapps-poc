using AppModels.approval;
using inventory.DomainModels;
using AutoMapper;

namespace inventory
{
    public class MapProfile : Profile
    {
        public MapProfile()
        {
            //CreateMap<ApprovalTodo, TodoListOutput>().ReverseMap();
        }
    }
}
